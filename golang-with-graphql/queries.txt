Procurar links e seus usuários 

query {
  links {
    title
    address
    user {
      	name
		}
  }
}

Criar um links

mutation create{
  createLink(input: {title: "something", address: "somewhere"}){
    title,
    address,
    id,
  }
}

